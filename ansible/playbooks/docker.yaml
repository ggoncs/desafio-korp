- name: Provisionamento do Ambiente
  hosts: proxmox_vm 
  become: yes
  gather_facts: yes
  
  tasks:
    - name: Verificar distribuição compatível
      assert:
        that:
          - ansible_os_family == "Debian"
        fail_msg: "Este playbook só suporta distribuições baseadas em Debian/Ubuntu"

    - name: Instalar dependências iniciais
      apt:
        name:
          - apt-transport-https
          - ca-certificates
          - curl
          - software-properties-common
          - python3-pip
          - rsync
        state: present
        update_cache: yes
      become: yes

    - name: Adicionar chave GPG do Docker
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present

    - name: Adicionar repositório do Docker
      apt_repository:
        repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable"
        state: present

    - name: Instalar Docker e Docker Compose
      apt:
        name:
          - docker-ce
          - docker-compose-plugin
        state: present
        update_cache: yes

    - name: Garantir que o serviço do Docker está rodando
      service:
        name: docker
        state: started
        enabled: yes

    - name: Garantir que o usuário existe
      user:
        name: "{{ ansible_user }}"
        state: present

    - name: Adicionar usuário ao grupo docker
      user:
        name: "{{ ansible_user }}"
        groups: docker
        append: yes
      when: ansible_user != 'root'
    
    - name: Reset de conexão
      meta: reset_connection
      when: ansible_user != 'root'
